# Android Capacitor Build & CI/CD Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Wrap F-Guild Next.js PWA in Capacitor for Android with CI/CD pipeline that builds debug APK and uploads to Firebase App Distribution.

**Architecture:** Capacitor WebView loads the deployed web app URL. Android project is a thin native shell. GitHub Actions builds APK on push to main and distributes via Firebase.

**Tech Stack:** Capacitor 6, Android (Gradle), Firebase App Distribution, GitHub Actions

---

## File Structure

| File | Responsibility |
|------|---------------|
| `capacitor.config.ts` (create) | Capacitor configuration — app ID, server URL, plugin settings |
| `scripts/set-capacitor-url.sh` (create) | Build-time script to swap server URL per environment |
| `dist/index.html` (create) | Empty placeholder required by Capacitor (unused — we load remote URL) |
| `android/` (generated) | Android project generated by `cap add android` |
| `android/app/src/main/res/mipmap-*/` (modify) | App launcher icons in all densities |
| `src/lib/capacitor-init.ts` (create) | Status bar / splash screen initialization for native platform |
| `src/components/capacitor/capacitor-provider.tsx` (create) | Client component that calls capacitor-init on mount |
| `src/app/[locale]/layout.tsx` (modify) | Add CapacitorProvider |
| `next.config.ts` (modify) | Update CSP for Capacitor WebView |
| `.gitignore` (modify) | Add Android build artifacts |
| `package.json` (modify) | Add Capacitor dependencies and scripts |
| `.github/workflows/android-build.yml` (create) | CI/CD pipeline for Android APK build + Firebase distribution |

---

### Task 1: Install Capacitor and initialize Android project

**Files:**
- Modify: `package.json`
- Create: `capacitor.config.ts`
- Create: `dist/index.html`
- Modify: `.gitignore`
- Generated: `android/`

- [ ] **Step 1: Install Capacitor packages**

```bash
pnpm add @capacitor/core @capacitor/cli @capacitor/splash-screen @capacitor/status-bar
```

- [ ] **Step 2: Create Capacitor config**

Create `capacitor.config.ts`:

```typescript
import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.fguild.app",
  appName: "F-Guild",
  webDir: "dist",
  server: {
    url: "https://f-guild.example.com",
    cleartext: false,
  },
  plugins: {
    SplashScreen: {
      launchAutoHide: true,
      launchShowDuration: 2000,
      backgroundColor: "#110b20",
      showSpinner: false,
    },
    StatusBar: {
      style: "DARK",
      backgroundColor: "#110b20",
    },
  },
};

export default config;
```

- [ ] **Step 3: Create dist placeholder**

Create `dist/index.html`:

```html
<!DOCTYPE html>
<html>
<head><title>F-Guild</title></head>
<body>Loading...</body>
</html>
```

- [ ] **Step 4: Add Android platform**

```bash
npx cap add android
```

Expected: `android/` directory created with full Gradle project.

- [ ] **Step 5: Create URL switcher script**

Create `scripts/set-capacitor-url.sh`:

```bash
#!/bin/bash
# Sets the Capacitor server URL for the target environment.
# Usage: ./scripts/set-capacitor-url.sh https://staging.f-guild.example.com
set -euo pipefail
URL="${1:?Usage: set-capacitor-url.sh <URL>}"
if [[ "$OSTYPE" == "darwin"* ]]; then
  sed -i '' "s|url: \"https\{0,1\}://[^\"]*\"|url: \"${URL}\"|" capacitor.config.ts
else
  sed -i "s|url: \"https\{0,1\}://[^\"]*\"|url: \"${URL}\"|" capacitor.config.ts
fi
echo "Capacitor server URL set to: ${URL}"
```

```bash
chmod +x scripts/set-capacitor-url.sh
```

- [ ] **Step 6: Update .gitignore**

Add to `.gitignore`:

```gitignore
# Android build artifacts
android/app/build/
android/.gradle/
android/build/
android/local.properties
android/app/src/main/assets/capacitorhash
*.apk
*.aab

# Capacitor dist placeholder
dist/
```

- [ ] **Step 7: Add npm scripts to package.json**

Add to `"scripts"` section in `package.json`:

```json
"cap:sync": "cap sync",
"cap:open:android": "cap open android",
"cap:build:android": "cap sync android && cd android && ./gradlew assembleDebug",
"cap:run:android": "cap run android"
```

- [ ] **Step 8: Verify Capacitor sync works**

```bash
npx cap sync android
```

Expected: No errors, `android/app/src/main/assets/public/` populated.

- [ ] **Step 9: Verify Android project builds**

```bash
cd android && ./gradlew assembleDebug 2>&1 | tail -5
```

Expected: `BUILD SUCCESSFUL` and APK at `android/app/build/outputs/apk/debug/app-debug.apk`.

- [ ] **Step 10: Commit**

```bash
git add capacitor.config.ts dist/index.html scripts/set-capacitor-url.sh android/ package.json pnpm-lock.yaml .gitignore
git commit -m "feat: add Capacitor with Android project"
```

---

### Task 2: Generate Android app icons from SVG

**Files:**
- Modify: `android/app/src/main/res/mipmap-mdpi/ic_launcher.png`
- Modify: `android/app/src/main/res/mipmap-hdpi/ic_launcher.png`
- Modify: `android/app/src/main/res/mipmap-xhdpi/ic_launcher.png`
- Modify: `android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png`
- Modify: `android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png`
- Same directories for `ic_launcher_round.png` and `ic_launcher_foreground.png`

Android requires PNG icons at specific densities. The source is `public/icons/icon-512.svg` (purple rounded rectangle with "FG" text).

- [ ] **Step 1: Install sharp for SVG-to-PNG conversion**

```bash
pnpm add -D sharp-cli
```

Or use a one-off Node script:

- [ ] **Step 2: Create icon generation script**

Create `scripts/generate-android-icons.mjs`:

```javascript
import sharp from "sharp";
import { mkdirSync, existsSync } from "fs";
import { join } from "path";

const ICON_SVG = "public/icons/icon-512.svg";
const MASKABLE_SVG = "public/icons/icon-maskable-512.svg";
const RES_DIR = "android/app/src/main/res";

const DENSITIES = [
  { name: "mdpi", size: 48 },
  { name: "hdpi", size: 72 },
  { name: "xhdpi", size: 96 },
  { name: "xxhdpi", size: 144 },
  { name: "xxxhdpi", size: 192 },
];

const FOREGROUND_DENSITIES = [
  { name: "mdpi", size: 108 },
  { name: "hdpi", size: 162 },
  { name: "xhdpi", size: 216 },
  { name: "xxhdpi", size: 324 },
  { name: "xxxhdpi", size: 432 },
];

async function generate() {
  for (const { name, size } of DENSITIES) {
    const dir = join(RES_DIR, `mipmap-${name}`);
    if (!existsSync(dir)) mkdirSync(dir, { recursive: true });

    await sharp(ICON_SVG)
      .resize(size, size)
      .png()
      .toFile(join(dir, "ic_launcher.png"));

    await sharp(MASKABLE_SVG)
      .resize(size, size)
      .png()
      .toFile(join(dir, "ic_launcher_round.png"));
  }

  for (const { name, size } of FOREGROUND_DENSITIES) {
    const dir = join(RES_DIR, `mipmap-${name}`);
    if (!existsSync(dir)) mkdirSync(dir, { recursive: true });

    await sharp(MASKABLE_SVG)
      .resize(size, size)
      .png()
      .toFile(join(dir, "ic_launcher_foreground.png"));
  }

  console.log("Android icons generated successfully.");
}

generate().catch(console.error);
```

- [ ] **Step 3: Run icon generation**

```bash
pnpm add -D sharp
node scripts/generate-android-icons.mjs
```

Expected: PNG files created in all `mipmap-*` directories.

- [ ] **Step 4: Verify icons exist**

```bash
find android/app/src/main/res/mipmap-* -name "*.png" | sort
```

Expected: 15 PNG files (5 densities x 3 icon types).

- [ ] **Step 5: Sync and rebuild**

```bash
npx cap sync android && cd android && ./gradlew assembleDebug 2>&1 | tail -3
```

Expected: `BUILD SUCCESSFUL`

- [ ] **Step 6: Commit**

```bash
git add android/app/src/main/res/mipmap-* scripts/generate-android-icons.mjs package.json pnpm-lock.yaml
git commit -m "feat: generate Android app icons from SVG"
```

---

### Task 3: Add Capacitor native initialization (status bar + platform detection)

**Files:**
- Create: `src/lib/capacitor-init.ts`
- Create: `src/components/capacitor/capacitor-provider.tsx`
- Modify: `src/app/[locale]/layout.tsx`

- [ ] **Step 1: Create Capacitor init module**

Create `src/lib/capacitor-init.ts`:

```typescript
import { Capacitor } from "@capacitor/core";

/**
 * Initialize native platform features (status bar, splash screen).
 * No-op on web — only runs inside Capacitor native shell.
 */
export async function initCapacitor(): Promise<void> {
  if (!Capacitor.isNativePlatform()) return;

  const { StatusBar, Style } = await import("@capacitor/status-bar");
  const { SplashScreen } = await import("@capacitor/splash-screen");

  await StatusBar.setBackgroundColor({ color: "#110b20" });
  await StatusBar.setStyle({ style: Style.Dark });
  await SplashScreen.hide();
}
```

- [ ] **Step 2: Create CapacitorProvider component**

Create `src/components/capacitor/capacitor-provider.tsx`:

```typescript
"use client";

import { useEffect } from "react";
import { initCapacitor } from "@/lib/capacitor-init";

export function CapacitorProvider() {
  useEffect(() => {
    initCapacitor().catch(() => {
      // Silently ignore — web browser doesn't have native plugins
    });
  }, []);

  return null;
}
```

- [ ] **Step 3: Add CapacitorProvider to root layout**

Modify `src/app/[locale]/layout.tsx`. Add import at the top:

```typescript
import { CapacitorProvider } from "@/components/capacitor/capacitor-provider";
```

Add `<CapacitorProvider />` after `<OfflineIndicator />` inside the `<body>`:

```tsx
        <RegisterSW />
        <OfflineIndicator />
        <CapacitorProvider />
```

- [ ] **Step 4: Verify build passes**

```bash
pnpm build 2>&1 | tail -5
```

Expected: Build succeeds (CapacitorProvider is a no-op on web).

- [ ] **Step 5: Commit**

```bash
git add src/lib/capacitor-init.ts src/components/capacitor/capacitor-provider.tsx src/app/\[locale\]/layout.tsx
git commit -m "feat: add Capacitor status bar and splash screen init"
```

---

### Task 4: Update CSP headers for Capacitor WebView

**Files:**
- Modify: `next.config.ts`

The Capacitor WebView loads the app from a remote URL. The current CSP `connect-src 'self'` blocks cross-origin API calls from the WebView. We need to allow Capacitor-specific schemes and the app's own domain.

- [ ] **Step 1: Update CSP in next.config.ts**

Modify the `securityHeaders` array in `next.config.ts`. Replace the current `Content-Security-Policy` header value:

Replace the existing CSP header object:

```typescript
  {
    key: "Content-Security-Policy",
    value: [
      "default-src 'self'",
      "script-src 'self' 'unsafe-inline' 'unsafe-eval'",
      "style-src 'self' 'unsafe-inline'",
      "img-src 'self' data: blob: https:",
      "font-src 'self'",
      "connect-src 'self'",
      "frame-ancestors 'none'",
      "base-uri 'self'",
      "form-action 'self'",
    ].join("; "),
  },
```

With:

```typescript
  {
    key: "Content-Security-Policy",
    value: [
      "default-src 'self'",
      "script-src 'self' 'unsafe-inline' 'unsafe-eval'",
      "style-src 'self' 'unsafe-inline'",
      "img-src 'self' data: blob: https: capacitor:",
      "font-src 'self' https:",
      "connect-src 'self' https: capacitor: ws: wss:",
      "frame-ancestors 'none'",
      "base-uri 'self'",
      "form-action 'self'",
    ].join("; "),
  },
```

Changes:
- `connect-src`: added `https: capacitor: ws: wss:` — allows WebView to reach the API server and Capacitor bridge
- `img-src`: added `capacitor:` — allows loading images via Capacitor scheme
- `font-src`: added `https:` — allows loading fonts from remote origin

- [ ] **Step 2: Verify build passes**

```bash
pnpm build 2>&1 | tail -5
```

Expected: Build succeeds.

- [ ] **Step 3: Commit**

```bash
git add next.config.ts
git commit -m "feat: update CSP headers for Capacitor WebView"
```

---

### Task 5: Create GitHub Actions workflow for Android APK build

**Files:**
- Create: `.github/workflows/android-build.yml`

- [ ] **Step 1: Create the workflow file**

Create `.github/workflows/android-build.yml`:

```yaml
name: Android Build

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      server_url:
        description: "Server URL for the Capacitor WebView"
        required: false
        default: "https://f-guild.example.com"

concurrency:
  group: android-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-apk:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Set Capacitor server URL
        run: |
          URL="${{ github.event.inputs.server_url || secrets.CAPACITOR_SERVER_URL || 'https://f-guild.example.com' }}"
          ./scripts/set-capacitor-url.sh "$URL"

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            android/.gradle
          key: gradle-${{ runner.os }}-${{ hashFiles('android/**/*.gradle*', 'android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      - name: Build debug APK
        working-directory: android
        run: ./gradlew assembleDebug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: f-guild-debug-${{ github.sha }}
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30

  distribute:
    needs: build-apk
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: Download APK artifact
        uses: actions/download-artifact@v4
        with:
          name: f-guild-debug-${{ github.sha }}

      - name: Upload to Firebase App Distribution
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_APP_ID }}
          serviceCredentialsFileContent: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          groups: internal-testers
          file: app-debug.apk
          releaseNotes: |
            Build from commit ${{ github.sha }}
            Branch: ${{ github.ref_name }}
            Message: ${{ github.event.head_commit.message }}
```

- [ ] **Step 2: Verify workflow syntax**

```bash
cat .github/workflows/android-build.yml | head -3
```

Expected: `name: Android Build`

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/android-build.yml
git commit -m "feat: add GitHub Actions workflow for Android APK build and Firebase distribution"
```

---

### Task 6: Firebase setup instructions and final verification

**Files:**
- Create: `docs/firebase-setup.md`

This task documents the manual Firebase Console steps that must be done by a human (cannot be automated in code).

- [ ] **Step 1: Create Firebase setup guide**

Create `docs/firebase-setup.md`:

```markdown
# Firebase App Distribution Setup

## 1. Create Firebase Project

1. Go to [Firebase Console](https://console.firebase.google.com/)
2. Click "Add project" → name: `f-guild`
3. Disable Google Analytics (not needed for App Distribution)
4. Click "Create project"

## 2. Register Android App

1. In the Firebase project, click Android icon (or "Add app")
2. Package name: `com.fguild.app`
3. App nickname: `F-Guild Android`
4. Debug signing certificate SHA-1:
   ```bash
   cd android && keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android 2>/dev/null | grep SHA1
   ```
5. Click "Register app"
6. Download `google-services.json` → place in `android/app/`
7. Skip "Add Firebase SDK" steps (not needed for App Distribution only)

## 3. Enable App Distribution

1. In Firebase Console sidebar → "Release & Monitor" → "App Distribution"
2. Click "Get started"
3. Create tester group: `internal-testers`
4. Add tester emails to the group

## 4. Create Service Account for CI

1. Go to [Google Cloud Console](https://console.cloud.google.com/) → IAM → Service Accounts
2. Select the Firebase project
3. Create service account: `github-actions-firebase`
4. Role: `Firebase App Distribution Admin`
5. Create JSON key → download

## 5. Add GitHub Secrets

In the GitHub repository → Settings → Secrets and variables → Actions:

| Secret | Value |
|--------|-------|
| `FIREBASE_APP_ID` | From Firebase Console → Project Settings → Your Apps → App ID (e.g., `1:123456789:android:abc123`) |
| `FIREBASE_SERVICE_ACCOUNT` | Full JSON content of the service account key file |
| `CAPACITOR_SERVER_URL` | Deployed web app URL (e.g., `https://f-guild.example.com`) |

## 6. Test the Pipeline

1. Push to `main` branch
2. Go to GitHub Actions → "Android Build" workflow
3. Or trigger manually: Actions → "Android Build" → "Run workflow"
4. APK will be uploaded to Firebase App Distribution
5. Testers receive email invitation to download

## 7. Install on Android Device

Testers:
1. Check email for Firebase App Distribution invite
2. Install "Firebase App Tester" app from Play Store
3. Accept the invitation
4. Download and install the APK
```

- [ ] **Step 2: Run full project verification**

```bash
pnpm build 2>&1 | tail -5
```

Expected: `BUILD SUCCESSFUL` or similar Next.js success output.

```bash
pnpm lint 2>&1 | tail -3
```

Expected: No errors.

```bash
pnpm type-check 2>&1 | tail -3
```

Expected: No errors.

- [ ] **Step 3: Verify Capacitor sync + Android build**

```bash
npx cap sync android 2>&1 | tail -5
cd android && ./gradlew assembleDebug 2>&1 | tail -5
```

Expected: Both succeed.

- [ ] **Step 4: Commit**

```bash
git add docs/firebase-setup.md
git commit -m "docs: add Firebase App Distribution setup guide"
```

---

## Summary

| Task | Description | Dependencies |
|------|-------------|-------------|
| 1 | Capacitor init + Android project | None |
| 2 | Android app icons | Task 1 |
| 3 | Status bar + platform detection | Task 1 |
| 4 | CSP headers for Capacitor | None |
| 5 | GitHub Actions CI/CD workflow | Task 1 |
| 6 | Firebase setup docs + verification | Tasks 1-5 |
